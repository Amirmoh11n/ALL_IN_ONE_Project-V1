# src/evaluate

Runs the trained model against the untouched Testing set and reports the evaluation metrics
(Confusion Matrix, Recall, F1-macro, Precision, ROC-AUC, Accuracy) defined in `src/metrics`.
