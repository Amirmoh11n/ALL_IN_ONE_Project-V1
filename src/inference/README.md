# src/inference

The inference pipeline: loads a trained/exported model and returns a class prediction for a single input image.
This is what the FastAPI backend calls when a user uploads an MRI image.
