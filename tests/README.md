# tests

Unit/integration tests, one focused test module per source module where practical (many small, focused tests
rather than a few large ones), used as a safety net for refactoring and regression prevention.
